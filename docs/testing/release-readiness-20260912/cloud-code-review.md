# Final Cloud release code review — 2026-09-12

Baseline: origin/main cbceb52715e50189094500539923288f61c95e9a versus current working tree, including authorized uncommitted release fixes. Read-only diff review followed by one explicitly authorized protocol-selection fix. This is a risk-focused review of the server/API groups below; it is not a claim that every generated/public asset was individually reviewed.

## Actionable finding and resolution

**P2 — fixed: available mixed-protocol pools could advertise an unusable protocol.** Previously src/services/provider-store-postgres.mjs:201 selected the first enabled endpoint's protocol regardless of health or price coverage; memory had the equivalent modelView choice. Availability counted eligible healthy endpoints across protocols. An unhealthy first Anthropic endpoint plus healthy OpenAI endpoint therefore advertised available=true/apiFormat=anthropic, and hosted Agent dispatch failed503 while a healthy alternative existed. protocol-review-repro.mjs and protocol-review-repro.log captured deterministic pre-fix reproduction. Read-only UAT SQL found no currently enabled mixed-protocol pools; actual production pool configuration remains unverified because AWS credentials expired.

Fixed in provider-store-postgres.mjs:195–220: a shared covered-provider and healthy-provider SQL predicate now drives both availability counts and ordered protocol selection. Fixed in provider-store-memory.mjs:78–107: the same healthy eligible provider list drives counts and protocol selection. Priority/id determinism and configured-protocol display fallback for unavailable pools remain. No post-dispatch cross-protocol retry and no price or security requirement weakened.

New test/provider-protocol-availability.test.mjs exercises memory and real PostgreSQL: unhealthy first, unpriced first, price above envelope, stale health, open circuit, reversed protocols, healthy priority choice, and none eligible. Public catalog, admin model view, executionModel and actual router selection agree. Command: PENECHO_TEST_DATABASE_URL=<exact disposable loopback DB> node --test test/provider-protocol-availability.test.mjs test/provider-pool.test.mjs test/provider-request-lease.test.mjs. Result55passed0failed0skipped, log protocol-regression.log. Fresh isolated DB penecho_billing_test_protocol_20260912 ran all68 migrations and was dropped afterward. New file adds one default environment-dependent PG skip; it already passed with actual PG.

## Review coverage

| Group | Concrete evidence inspected | Assessment |
| --- | --- | --- |
| Browser authentication / early upload admission | src/http/auth-context.mjs loadBrowserAccount diff, requireAccount/CSRF/hidden administrator guards, src/routes/hosted-document-files.mjs onRequest; device-sync/local-access response changes | Early account loading retains browser session-kind check and account cache decoration; other changed auth responses only add membership. No new bypass found. |
| Hosted Agent WebSocket ownership and lifecycle | src/routes/hosted-canvas-agent.mjs origin/account/assertCanvas principal, queued initialization bounds, heartbeat/disconnect cleanup; src/services/canvas-agent/hosted-runtime.mjs principal and fileResources wiring; relevant upstream runtime attachment admission changes | Route retains server-owned account/canvas principal and exact allowed origins. Heartbeat owns ping/pong interval and disconnect is once-only. Real UAT doc cross-user/cross-conversation denials plus heartbeat/store tests from full suite provide evidence. |
| Native Canvas CRUD | src/routes/native-canvas.mjs account/write guards, assertCanvas, base-revision save, repository calls; existing unchanged canvas-bundle-flow contract | Changed diff is principally Widget fetch integration; CRUD still validates account ownership, CSRF and base revision. Real PG object-deletion suite verifies moved canvas ownership and historical object cleanup. |
| Viewer/Widget public fetch | new routes/viewer-widget-fetch.mjs, services/viewer-widget-fetch.mjs, replacement services/widget-fetch.mjs, canonical upstream public-fetch.js DNS/address/redirect path | Public route rechecks publication/free-license access each request. GET-only public HTTPS fetch, blocked local/private addresses, pinned DNS and redirect revalidation retained. Cookies/provider credentials not forwarded. Response sandbox/disposition headers and disconnect cancellation present. Shared process queue/concurrency replaces prior account allowlist/rate policy, matching documented intended feature expansion. |
| Native inference protocols/streaming | routes/hosted-chat.mjs, hosted-native-schema.mjs, services/hosted-chat.mjs, hosted-command.mjs, provider-executor.mjs, provider-http.mjs, provider-chat-stream.mjs, hosted-adapter.mjs | Both protocols retain hosted auth and one shared limiter. Request digest includes protocol. Tool calls stay sealed until completion; progress streaming has bounds, usage/terminal validation and abort cleanup. Provider lease renewal is independent of model-progress timeout. One protocol advertisement defect fixed above. |
| Reservations / storage / deletion | provider-store memory+PG renewal diffs, hosted-request-lease.mjs, account-deletion resource hook; migrations065–068; previous billing/document review | User-owned live dispatch lease update is atomic and does not alter credits; source-token accounting remains single owner. All prior12 actual PG finance/storage tests plus remaining13 PG/Redis checks passed. Account deletion marks file resources deleting before owner teardown; actual document deletion/revocation passed. |
| Feedback / administrator surfaces | repository deleteFeedback memory+PG, routes/feedback.mjs, routes/admin-providers.mjs | Feedback deletion binds both ID and owner, CSRF required; admin model change only output-limit default. Real PG feedback/administrator checks passed. |
| Production desired configuration | config.mjs, infra/low-cost ECS/worker/variables, deployment workflow, bootstrap policies and dedicated tests | Worker image digest, private service discovery, token secret and feature flags now wired; public verified production agent/native flags preserved by defaults. Terraform validate/fmt,32 deployment tests and workflow bash parsing passed earlier. Actual IAM application remains pending. |

## Release prerequisites and coverage limits

- Repo desired configuration passes validation. Actual production ECS environment, IAM policies and secrets cannot be declared reconciled: available AWS session was expired. Before authorized production release, refresh AWS authentication, apply updated scoped bootstrap IAM policies once, and re-run production config/preflight. No production mutation performed here.
- Direct read-only live Stripe SDK preflight passed; comprehensive AWS-backed production config preflight remains blocked on expired AWS session. Actual test-mode paid subscription/webhook/idempotency/credits/membership and hosted spend passed; paid credit-pack checkout completion was not performed through browser or real payment.
- Default full Cloud suite before latest protocol test:759 tests,733pass26 environment skips,0fail with bounded concurrency; all26 skips have independent real integration passes (12PG +13PG/Redis +1private-worker). Added protocol regression introduces one additional default PG skip, also independently passed. Parent owns final full-suite count for the final integrated tree.
- Real document acceptance covers TXT/CSV/PDF/DOCX; XLSX was not additionally exercised. Worker handles actual fixture extraction, not OCR/scanned PDF acceptance.
- Generated public Canvas assets and imported runtime are covered by parent sync/desktop/frontend review and tests; this report does not certify individual generated-file inspection.

No additional unresolved source release blocker was identified in these reviewed groups after the protocol fix. The remaining concrete production release dependency is actual environment/IAM reconciliation, documented separately in prod-readonly.md.
