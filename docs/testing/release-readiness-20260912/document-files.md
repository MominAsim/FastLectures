# UAT document acceptance — 2026-09-12

PASS. Actual Cloud HTTP upload and hosted Agent WebSocket read used UAT 127.0.0.1:18082 with existing Cognito-mode session machinery and new synthetic users. No authentication behavior changed. Real hosted model invocation incurred UAT credits/API usage. All four formats returned exact fixture markers via read_attachment: TXT ORCHID314, CSV MAPLE271, PDF BIRCH618, DOCX CEDAR161. These markers were absent from the user prompt. Real private worker was used via configured service, not a stub.

Isolation: different conversation rejected existing file; other synthetic account rejected read and DELETE (404); owner DELETE returned204 for all4; same original conversation then rejected deleted file. See evidence.json,read-frames.json,conversation-denied.json,revocation.json. DOCX satisfies requested DOCX/XLSX coverage; XLSX was not additionally exercised.

Cleanup: four documents from successful run plus four from initial harness correction deleted204. All four exact synthetic canvases from both runs trashed and permanently deleted204 (cleanup.json); exact projects were removed by normal DELETE200; subsequent permanent404 indicated absence, confirmed via database. cleanup-db.log confirms no remaining canvases for four exact synthetic users. Redis sessions revoked; private credential file removed. Synthetic account and usage ledger records retained for audit. Initial harness-only corrections: hosted hello projectId must be empty (host-local projects unsupported); bodyless DELETE must omit JSON content-type. No product source defect identified.

Generic worker integration test also passed1/1,0skips using worker-test.mjs bridge to already-running authenticated private UAT worker. The localhost-only bridge forwards only document headers and obtains worker token inside app container without printing secrets. It was closed after test.

Remaining environment integrations: remaining-integration.log in parent evidence directory records13passed0failed0skipped: administrator role1, feedback1, object deletion7, provider health1, test account1, operational retention PG1+Redis1. All68 migrations applied through068_credit_zero_floor.sql. Isolated newly created database fastlectures_billing_test_release_20260912_remaining was dropped afterward. Redis used dedicated new disposable container fastlectures-release-retention-20260912 on loopback16389, removed afterward; shared UAT Redis untouched. Five tests had only exact database-name guard widened to same strict disposable naming convention already authorized, existing hostname checks unchanged.

All26 default Cloud suite environment skips now have concrete independent passing evidence: prior12PG + current13PG/Redis +1real-worker. Default full suite without environment still reports26skips; these independent runs are not a claim that default npm check ran with zero skips.
